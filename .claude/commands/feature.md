Implement the requested Referral feature end to end using the `referral-feature` project skill.

Start by reading `AGENTS.md`, tracing the existing contract and stating the behavioral invariants. Keep `/` and `/ref/:code` in the user app, `/admin/` in the operations console, and reusable clients/types in shared packages. Add focused tests, run proportional backend and frontend checks, and record any durable decision as an Agent Note.
