Investigate the reported Referral defect using the `referral-investigate` project skill.

Reproduce it, collect sanitized evidence, trace the request across routing, browser client, middleware, controller, service, repository, and data, then identify the first broken boundary. Do not change code unless the request includes a fix. If fixing, add a regression test and verify the affected app.
